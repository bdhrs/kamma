# Kamma Context

If a user mentions a "plan" or "thread", they likely refer to one of the thread plans in `kamma/threads/<thread_id>/plan.md`.

## Loop Threads

If the user mentions creating or working on a "loop" (a standing thread for repeated feedback loops or issue processing), refer to `loop-design.md` (located alongside this file) for the specialized workflow.
